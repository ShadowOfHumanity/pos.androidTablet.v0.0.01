<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JakePOSv001"
        tools:targetApi="31">

        <activity android:name=".ActivityMain"
            android:exported="true"> <!-- Main Activity -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ActivityBeverages" /> <!-- Beverage Activity -->
        <activity android:name=".ActivityBeveragesHot" /> <!-- Beverage Hot Activity -->
        <activity android:name=".ActivityBeveragesCold" /> <!-- Beverage Cold Activity -->
        <activity android:name=".ActivityExtra" /> <!-- Extra Activity -->
        <activity android:name=".ActivitySelectVoid" /> <!-- Remove Activity -->
        <activity android:name=".ActivityFinishOrder" /> <!-- FinishOrder Activity -->
        <activity android:name=".ActivityTodayTotal" /> <!-- FinishOrder Activity -->
        <activity android:name=".ActivityFood" /> <!-- Food Activity -->
    </application>
</manifest>
